# Paperclip 本地化二次开发计划

日期：2026-09-24

目标：以 `H:\project\Papercilp` 作为唯一源码工作区，持续同步上游、保留简体中文能力，并让 `http://127.0.0.1:3100` 始终可用于验证当前开发版本。

## TODO

- [x] Fork `paperclipai/paperclip` 到 `lidaichao/paperclip`，克隆最新版并配置 `upstream`。
- [x] 定位旧部署、运行数据、旧中文源码和中文维护工具，固化未提交成果。
- [x] 将中文维护工具迁入本仓库，并在最新上游源码上重新盘点和重放翻译。
- [x] 完成中文专项测试、类型检查和生产构建（27 项测试通过；新增上游文案暂以英文回退）。
- [x] 建立隔离开发启动入口：3101 使用仓库内 `.paperclip-dev`，拒绝连接生产数据库。
- [ ] 保持 3100 继续使用 `H:\AIagent\Luna\.paperclip`；新版切换前必须先备份并获得明确确认。
- [ ] 将启动/停止/更新脚本改为指向唯一源码工作区，验证修改后可快速重启。
- [ ] 删除已迁移的旧源码副本；保留数据库、配置、密钥、附件和必要备份。
- [ ] 提交并推送开发分支，为后续二次开发建立固定工作流。

## 当前安全边界

- 生产数据目录 `H:\AIagent\Luna\.paperclip` 不迁移、不覆盖、不执行数据库升级。
- 本轮只验证源码、类型和构建；3100 的生产切换留到完成备份并获得明确确认之后。
- 开发验证使用 3101 和仓库内 `.paperclip-dev`，与生产数据库物理隔离。

## 工作约定

1. `origin` 指向个人 fork，`upstream` 指向官方仓库。
2. 日常开发只在 `codex/zh-cn-development`（后续可改名）进行，不直接修改 `master`。
3. 更新顺序固定为：备份数据 → 拉取上游 → 重放/补齐翻译 → 测试与构建 → 重启 3100 → 浏览器验收。
4. 运行数据不进入 Git；源码目录和 `H:\AIagent\Luna\.paperclip` 数据目录分离。
5. 任何数据库迁移都先生成可恢复备份，再运行新版服务。
