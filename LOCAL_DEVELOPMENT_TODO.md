# Paperclip 二次开发待办

- [x] Fork 上游仓库并拉取到 `H:\project\Papercilp`
- [x] 合入现有中文本地化改动并通过 UI、类型和测试检查
- [x] 冷备份现用数据并校验文件及 SQL 备份
- [x] 用当前源码对应的运行时和本地化 UI 升级 `127.0.0.1:3100`
- [x] 验证健康检查、中文界面、公司/项目/任务数据不变
- [x] 固化前端/本地化的开发→构建→同步→验证脚本
- [ ] 固化后端变更的打包→备份→迁移→切换→回滚脚本

## 安全约束

- 生产数据目录固定为 `H:\AIagent\Luna\.paperclip`。
- 每次升级前必须先生成并校验冷备份。
- 不得用新建数据库或测试数据覆盖生产数据。
- 只有新版本健康检查和数据校验通过后，才将其标记为活动版本。
