# 中文浏览器验收记录

- 日期：2026-09-03。
- 环境：开发预览 `http://127.0.0.1:3101`，后端保持 `3100`。
- 浏览器：独立 Edge 标签。子代理未获得 in-app 浏览器能力，未操作主代理标签。
- 操作边界：浏览、切换展示模式、打开并关闭空白新建任务窗口；未提交任务、修改业务数据或运行智能体。
- 当前状态：指定页面和中英文切换均已完成检查；尾轮真实浏览器复验 B01–B11 全部通过，最后一项成本顶部预算卡片已显示“无限制”。

## 已检查

| 页面 | 结果 |
|---|---|
| 公司常规设置 | 主标题、设置字段、权限说明、备份保留策略、退出与归档入口中文；主表单无明显截断。末轮确认 Ready/None 显示为“已就绪／无”。 |
| 任务看板 | 七列状态中文，3 张已有任务卡片标题与标识保留，路由仍为 `/LUN/issues/LUN-*`；看板横向滚动属于原布局，未发现因中文造成的重叠。 |
| 新建任务窗口 | 主标题、占位提示、负责人/项目控件、状态/上传/创建按钮中文；未填写或提交。“用于”标签发生两行折行，已作最小布局修复。 |
| 智能体列表 | 状态标签、时间、工具栏中文；用户名、模型名称、用户职位内容保留。数量误带“秒”已修复，复验为“5 智能体”。 |
| Luna 智能体详情仪表盘 | 标签页、图表标题、近期任务、运行状态中文；日期显示为 `2026年9月3日`，费用保持 `$0.00`；既有任务输出原样显示。 |
| 智能体配置 | 配置字段、选项、权限说明为中文，命令路径、工作目录、CODEX_HOME、模型标识及附加参数保留。末轮面包屑和页面标题均为“配置”，编辑器 aria 为“可编辑的 Markdown 内容”。 |
| 项目列表与详情 | 项目名称／描述等用户内容保留，列表状态及详情任务标签中文；末轮 My Projects 为“我的项目”，Leave 为“退出”。 |
| 收件箱 | 我的／最近／未读／受阻／全部标签页、任务状态、归档按钮与相对时间中文；未执行归档。 |
| 成本 | 费用、财务账本、日期筛选、提供商等中文；金额保持美元。末轮“输入／输出”正确，顶部预算卡片已显示“无限制”。 |
| 技能 | 导航、筛选、状态、数量为中文，卡片网格无重叠；官方技能原始内容保留英文。末轮排序显示“智能体数最多”，系统文件夹显示“未分类”。 |
| 账户菜单与语言切换 | 真实选择 English 后刷新，导航恢复英文且组织名／路由保持原样；再次选择简体中文后刷新，导航恢复中文。最终保留中文。 |

## 发现项

| 编号 | 级别 | 位置和实际显示 | 建议／状态 |
|---|---|---|---|
| B01 | 已修复 | Vite 报 `ExecutionWorkspaceCloseDialog.tsx` 第 1、2 行重复声明 `enumLabel`；刷新后整个预览无法加载。 | 主代理统一去重，刷新后恢复正常。属于开发期间热更新状态，已复验。 |
| B02 | 已修复 | 智能体列表显示“5 智能体 秒”。 | 主代理修复后，真实浏览器显示“5 智能体”，不再多出“秒”。 |
| B03 | 已修复 | 新建任务窗口的“用于”折成两行。 | 已将 `NewIssueDialog.tsx:1502` 的 `w-6 shrink-0 text-center` 改为 `min-w-6 shrink-0 whitespace-nowrap text-center`；主代理已纳入重放。截图复验为单行，无重叠。 |
| B04 | 已修复 | 组织切换器曾显示 organization。 | 末轮浏览器确认“打开 Luna · Codex 团队 组织 切换器”。 |
| B05 | 已修复 | 侧栏 `Agents section actions`、`New agent`；智能体卡片 `Leave Luna` 等。 | 末轮确认“智能体分区操作”“新建智能体”“退出 Luna”。 |
| B06 | 已修复 | Markdown 编辑器 aria 曾为 `editable markdown`。 | 主代理接入编辑器翻译回调；末轮配置能力编辑器确认“可编辑的 Markdown 内容”，中文占位描述正常。 |
| B07 | 已修复 | 设置 `Ready`／`None`；隐藏命令面板 `Command Palette`／`Search for a command to run...`。 | 末轮确认“已就绪／无”“命令面板／搜索要运行的命令…”。 |
| B08 | 已修复 | 项目 `My Projects`，配置页 `Configuration`，成本预算 `Open`。 | 末轮“我的项目”和标题／面包屑“配置”通过；最后刷新 `/LUN/costs` 确认顶部预算卡片显示“无限制”，下方为“未配置月度上限”。 |
| B09 | 已修复 | 成本输入词元 `in` 曾显示为“位于”。 | 末轮按智能体统计显示“输入 753.2k · 输出 3.8k”，美元符号和金额保持。 |
| B10 | 已修复 | 技能 `Most agents`／`Unfiled` 语境错误。 | 末轮确认“智能体数最多”“未分类”。 |
| B11 | 已修复 | 账户 `View profile`／`Edit profile` 曾误译成配置方案。 | 末轮确认“查看个人资料／编辑个人资料”，链接路由仍为原 `/u/board` 和设置 profile 路径。 |
| B12 | 原始内容 | 官方随包技能的描述与来源 `Paperclip bundled` 仍为英文。 | 属于后端技能元数据／技能内容，不应直接改写技能正文；可另做仅展示的中文说明层。已向主代理明确此范围。 |

## 后续收口

- B01–B11 发现项已复验通过，浏览器范围内无未处理的界面漏译或语境错误。
- 本轮只覆盖当前真实数据可到达的指定页面，不代表所有插件、错误分支、空状态均经过浏览器验证。

截图已通过 CUA 在会话中查看：设置、看板、空白新建任务窗口（修复前后）、智能体列表、技能卡片。未将截图复制到其他位置。
